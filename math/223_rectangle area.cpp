#include <iostream>

using namespace std;

class Solution
{
public:
	int computeArea(int A, int B, int C, int D,
		int E, int F, int G, int H)
	{
		for ()
	}
private:
	bool inArea(int A, int B,
		int E, int F, int G, int H)
	{
		if ((A > E) && (A < G) && (B > F) && (B < H))
		{
			return true;
		}
		else 
			return false;
	}
};